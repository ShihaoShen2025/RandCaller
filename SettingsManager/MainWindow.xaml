<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SettingsManager.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SettingsManager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SettingsManager">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!-- 左侧菜单 -->
        <ListBox Grid.Column="0" Margin="0,0,8,0" SelectedIndex="0">
            <ListBoxItem Content="常规设置"/>
            <ListBoxItem Content="高级设置"/>
        </ListBox>
        <!-- 右侧设置项 -->
        <StackPanel Grid.Column="1" Margin="24">
            <TextBlock Text="示例设置项" FontSize="16" Margin="0,0,0,8"/>
            <ToggleSwitch x:Name="ExampleSwitch" Header="启用示例功能"/>
        </StackPanel>
    </Grid>
</Window>
