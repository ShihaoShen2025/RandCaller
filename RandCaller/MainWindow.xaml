<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="RandCaller.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RandCaller"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="RandCaller">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="24,0,0,0">
            <Button x:Name="DrawButton"
                    Content="抽选"
                    Width="64" Height="32"
                    Style="{StaticResource AccentButtonStyle}"/>
            <Button x:Name="MoreButton"
                    Content="⋮"
                    Width="32" Height="32"
                    Margin="8,0,0,0">
                <Button.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="示例菜单项1"/>
                        <MenuFlyoutItem Text="示例菜单项2"/>
                    </MenuFlyout>
                </Button.Flyout>
            </Button>
        </StackPanel>
    </Grid>
</Window>
